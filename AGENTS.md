# GPT Pro 项目约束

本仓库用于维护单人、多地点访问一个 ChatGPT Pro 登录态的自托管工作台。目标架构是一份持久化 Chromium Profile、一次 ChatGPT 登录、多个相互独立的逻辑工作区；每个工作区使用独立的网关凭据和页面会话。

## 范围与边界

- 日常入口按逻辑工作区隔离页面画面、输入、文件传输和恢复状态；不同工作区共享同一 Chromium Profile 与 ChatGPT 登录态。
- 用户、密码和工作区 Target 必须由持久化数据动态管理，不得把具体使用场景或固定数量写进业务分支；资源允许时可增减任意数量的用户与工作区。
- 当前容量回归基线是十二个用户各绑定一个独立工作区，十二个窗口可同时在线；该数量是验收下限而不是业务上限。
- 时区、浏览器语言与 `Accept-Language` 是唯一 Chromium Profile 的全局一致性设置；可经 Chromium 实际出口 IP 自动探测并允许管理员覆盖，但不得按工作区伪装成多套环境，不得持久化或记录公网 IP。
- 完整 KasmVNC 浏览器桌面只允许管理员访问，用于登录、退出、MFA、扩展管理、Profile 全局设置、故障恢复和调试，不作为普通工作区的并发访问通道。
- 管理页的浏览器连接状态、工作区数量、在线窗口、管理员浏览器入口和登出必须集中在一个系统功能区；Projects 读取结果及重复的工作区、用户和文件条目默认折叠，只展开当前查看或编辑项，并通过列表最大高度避免继续拉长整页。
- 普通工作区必须通过管理员可配置的敏感操作黑名单限制账号、订阅、安全、全局设置和破坏性操作；控件先隐藏，点击与键盘激活在网关发送 CDP `Input` 前检查，明确 URL 黑名单再由 Chromium 阻断。不得暂停、继续或改写普通文档请求，也不得在页面安装捕获阶段事件拦截器；管理员浏览器不注入普通工作区规则。
- 普通工作区只显示项目画布、常驻的项目首页按钮和默认折叠的控制面板，不保留永久顶栏、底部提示或项目自建输入法控件；折叠菜单依次为上传文件、查看文件、进入全屏、刷新页面、退出登录。本地网页标题使用工作区名称，管理页和管理员浏览器标题统一为 `GPT Pro`。
- 受管页面必须隐藏 ChatGPT 网页侧边栏、左上角项目名称/图标入口、项目菜单、分享、Chat/Work 切换、听写与语音入口；这些固定限制覆盖项目首页、具体对话和单条消息，普通用户不可关闭。识别到项目起始 URL 时，项目首页必须能进入或新建其项目对话，不得假设对话 URL 的 `g-p` 标识与项目首页相同；进入对话后必须可通过本地常驻按钮返回配置的项目首页。该能力是可观察的 UI 与访问控制，不得宣称网页无法探查、规避风控或对未来 ChatGPT 结构永久有效。
- 本地上传只保存到当前用户的私人目录，不得自动交给 ChatGPT；只有用户在 ChatGPT 页面手动打开文件选择器并在私人文件面板确认后，才能处理该次选择。远端下载只通过独立传输目录流通；gateway 不得为此挂载完整 Chromium Profile。声音、麦克风和语音模式不在支持范围内。
- 普通客户端不得直接访问 Chromium DevTools、VNC 内部凭据、Docker Socket 或其它工作区的页面目标。
- 用户名、密码、ChatGPT Cookie、Profile、会话令牌、代理凭据和真实项目 URL 不得写入仓库、测试快照、日志或可提交样例。
- 本地网关密码按用户要求不设最短长度；仍须拒绝空白密码和超过 256 字符的输入，界面、预设管理员与用户管理路径必须使用同一规则。
- 普通工作区必须把访问设备本机浏览器产生的可打印文本、dead key、粘贴和 IME 组合留在本机处理；候选中间值不得发送。普通文字和 `compositionend` 的确认文本通过所属 CDP `sessionId` 的 `Input.insertText` 提交；本机 `insertFromPaste` 文本必须经跨工作区全局原子队列写入远端 Chromium 剪贴板并执行一次原生 Paste，不得把大段多行粘贴交给单次或循环 `Input.insertText`。远端纯文本选区只允许映射到发起选区请求的本地窗口，`Cmd/Ctrl+C` 与 `Cmd/Ctrl+X` 使用该窗口的浏览器原生剪贴板事件，禁止跨用户广播或程序化读取本机剪贴板。ChatGPT 原生消息复制按钮必须保留；按钮写入远端系统剪贴板后，由同一队列按 `clipboardchange` 顺序读取，并且只写入触发按钮的本机窗口剪贴板，不得轮询或按时间猜测归属。macOS 的本机 `Command` 组合必须映射为远端 `Control`，其它平台的本机 `Control` 继续映射为远端 `Control`；本机复制、剪切和粘贴快捷键仍由本机浏览器处理。Enter、删除、导航和其它远端快捷键继续走键盘事件。管理员完整桌面只启用 KasmVNC 原生 IME Input Mode，不得再叠加语言按钮、内置词典、候选服务、Fcitx 或项目自定义切换快捷键。

## 实现原则

- 只实现当前已验证契约所需的一套路径；禁止为假设中的旧版本、备用格式、未知页面结构或未出现故障增加兼容分支、fallback、反射探测、过度防御、冗余状态、重复日志与解释性注释。新机制覆盖旧机制时沿代码、配置、测试和文档一起删除旧路径。
- 以工作区 ID 作为显式路由和授权边界，禁止依赖一个全局“当前桌面”Cookie 切换目标。
- 持久化工作区配置与最后页面 URL；Chromium `targetId`、CDP `sessionId` 等运行期标识不得落盘。
- 浏览器重启后应从持久化配置重建工作区页面；恢复失败必须显式提示，不得静默跳到其它工作区。
- 每个工作区必须使用唯一的顶层 Chromium 窗口与独立 `Page.startScreencast` 流，不得退回逐帧 `Page.captureScreenshot` 或引入跨工作区全局串行锁。源帧及时确认，服务端只保留最后画面并对慢客户端丢弃过期帧，客户端合并待解码帧。
- 画面调度必须区分聚焦或最近交互窗口、后台可见窗口和没有可见观看者的窗口；默认交付上限分别为 60 FPS、8 FPS，静止画面每 2000 毫秒复用最后一帧保活。静止判断只使用 DOM 节点/文字、滚动、输入和尺寸活动信号，不得逐帧遍历页面；没有可见观看者时停止连续流并最小化远端窗口，后台渲染不得被 Chromium 的遮挡节流暂停。
- Chromium 默认下载目录必须固定在独立传输目录；若 CDP 下载行为被其它调试会话重置，只能接管完成事件报告且仍位于该目录内的普通文件，不得回退读取 Profile 的 `Downloads`。
- 错误处理应失败关闭：用户数据损坏、授权不明确、目标归属缺失或内部通道异常时拒绝访问。
- 用户可见启动脚本必须等待 desktop 与 gateway 健康后再打印入口，不得把容器仅进入 `Started` 或重启循环误报为启动成功。
- 输入桥接改动必须先用临时浏览器会话验证普通工作区的英文、中文组合、表情、原生粘贴、选区复制/剪切、ChatGPT 原生复制按钮、短间隔多窗口复制归属、特殊键与双页面状态隔离，再验证管理员 KasmVNC 原生 IME 的 Unicode 实际落入远端输入框；正式重建前后须以不读取 Cookie 内容的方式确认 Chromium Profile 与 ChatGPT 登录态数据仍存在。
- 代码、配置和 SVG 只保留解释安全边界、协议陷阱或平台差异所必需的注释；用户可见品牌统一使用 `GPT Pro`，不得重新引入旧品牌文案。仓库、镜像、容器和数据目录的既有小写技术标识不为品牌改名而迁移。

## 验证要求

- JavaScript 改动至少执行 `npm test`；网关依赖改动同时执行官方 npm registry 的生产依赖审计。
- Compose 或容器改动执行 `docker compose config --quiet`，并在真实 Docker 主机验证健康检查、重启恢复与持久化 Profile。
- CDP 转发或连接改动必须验证空闲时间超过建连超时后仍能创建 Target；转发器的建连超时不得残留为长连接读超时。
- Shell 改动执行匹配实际解释器的 `shellcheck`；Python 改动至少执行语法编译和相关直接测试。
- 多工作区实验必须验证：一次 ChatGPT 登录、十二个用户各自的十二个目标同时产帧、输入不串线、独立断线重连、Chromium 重启恢复、路径会话不互相覆盖。
- 权限与文件实验必须验证：普通工作区敏感点击和 URL 被拒绝、管理员浏览器不受普通工作区黑名单影响、用户私人上传互不可见、页面未真实触发 `Page.fileChooserOpened` 时不得调用 `DOM.setFileInputFiles`、用户必须手动选择并确认文件、远端下载只能由所属工作区或管理员取回。
- 并发画面实验必须验证十二个在线工作区使用独立顶层窗口连续流，并分别记录静止、一个前台加十一个后台、六个前台加六个后台、十二个前台和慢客户端背压下的有效帧率、CPU、内存、带宽与丢帧情况。必须区分配置上限、源帧采样与实际交付帧率，不得把 60 FPS 上限宣称为保证值。
- 未实际完成 ChatGPT 登录与双工作区运行验证时，不得宣称真实端到端验收通过。
